---
paths:
  - "docs/**"
---

Documentation scaling rules:
- Keep each doc file under 150 lines
- If a doc exceeds 150 lines, convert it to a folder with index.md + sub-pages
- index.md should be under 30 lines: summary + links to detail pages
- Only load the sub-page you need, not the whole folder
- CLAUDE.md must stay under 60 lines (routing map only)
